const CLICKAWAY = 'clickaway';
const DATE_FORMAT_TO_CHART = 'MMM DD h:mm';
const ALERTS_TYPE = {
  ph: 'ph',
  temperature: 'temperature',
};

export default {
  CLICKAWAY,
  DATE_FORMAT_TO_CHART,
  ALERTS_TYPE,
};
